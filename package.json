{
  "name": "ennera",
  "version": "1.1.0",
  "private": true,
  "description": "ENNERA — kontrol stopkontak pintar via Bluetooth (PWA offline)",
  "license": "MIT",
  "scripts": {
    "test": "node --test \"tests/*.test.js\""
  },
  "devDependencies": {
    "@sinonjs/fake-timers": "^11.2.2",
    "jsdom": "^24.1.0",
    "postcss": "^8.5.28"
  }
}
